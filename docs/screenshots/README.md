# Screenshot provenance

- `landing.png`: rendered from the running Next.js production landing route.
- `analysis-preview.png`: rendered from `/preview`, which loads the packaged measured pose and analytics artifacts used by `/analysis/demo`.
- `login.png`: rendered from the running authentication route.
- `pdf-report.png`: rendered from the measured PDF report.

Chromium navigation was restricted by the execution sandbox, so the running-server HTML was rendered through WeasyPrint. These are application renders, not manually designed mockups.
